# Hellas / Van Horn Eagles Stadium — 5-minute overview
# Starting FRESH from raw camera sources — the existing "Van Horn - v9"
# Resolve timeline has been reviewed and rejected (cuts aren't good), do
# NOT export/use v9 here. See notes.md for the multicam-sync-first step
# needed before raw_clip below can point at something real.
# raw_clip below is a placeholder path — replace it once the synced export
# exists on the machine actually running this pipeline.
job: hellas-van-horn
format: long-form-youtube   # short-explainer | short-tiktok-raw | long-form-youtube
raw_clip: projects/hellas-van-horn/raw/source.mp4

# Optional — omit either to skip that step
music: null   # e.g. projects/hellas-van-horn/assets/music/audiio-uplift-corporate.mp3

# whisperX settings, passed through to rough_cut.py
device: cuda   # cuda | cpu
model: large-v3
